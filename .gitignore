# Keep real local Firebase web config out of shared source control.
# The web config is not an admin secret, but it is project-specific.
firebase-config.js
.firebase/
.firebaserc
_preview.html

# macOS filesystem noise
.DS_Store
.fuse_hidden*

# Archives and local build artefacts
*.zip
node_modules/
_archive/

# Netlify deploy staging folders (root is the publish directory via netlify.toml)
sticker-swap-netlify-deploy/
sticker-swap-netlify-deploy-latest/

# Stale Codex worktree clones
codex-github-work-2/
codex-invite-landing-work/
